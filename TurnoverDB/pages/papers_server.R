# papers_server.R

# define server logic for Papers page

# sourced by app.R

# corresponding papers_ui.R




papers_server <- function(id){
  moduleServer(id, function(input, output, session){
    
    # server-side operations for Papers
    
    # (currently nothing is defined)
    
  })
}