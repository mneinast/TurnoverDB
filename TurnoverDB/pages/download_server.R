# download_server.R

# define server logic for Home page

# sourced by app.R

# corresponding download_ui.R




download_server <- function(id){
  moduleServer(id, function(input, output, session){
    # server-side operations for Download
    
    # (currently nothing is defined)
    
  })
}